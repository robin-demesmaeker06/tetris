# Tetris

A basic implementation of the classic retro arcade game **Tetris**, built using Python.

## 🎮 Features
* Classic Tetris gameplay mechanics.
* Score tracking and level progression.
* Customizable settings (grid size, colors, game speed).
* Clean code structure separating logic, settings, and utilities.

## 🛠️ Requirements
* **Python 3.x**
* **Pygame** (or the specific library you used)

## 🚀 How to Run

1.  **Clone the repository:**
    ```bash
    git clone [https://github.com/robin-demesmaeker06/tetris.git](https://github.com/robin-demesmaeker06/tetris.git)
    cd tetris
    ```

2.  **Install dependencies:**
    If you haven't installed `pygame` yet, run:
    ```bash
    pip install pygame
    ```

3.  **Start the game:**
    ```bash
    python main.py
    ```

## 🕹️ Controls
*(Update these if your specific controls are different)*

* **Left / Right Arrow:** Move block
* **Up Arrow:** Rotate block
* **Down Arrow:** Soft drop (move down faster)
* **Space:** Hard drop (instant placement)
* **Esc:** Pause / Quit

## 📂 Project Structure

* `main.py`: The entry point of the game. Handles the main game loop.
* `settings.py`: Contains configuration variables (screen dimensions, colors, block shapes).
* `utils.py`: Helper functions used throughout the game logic.

## 🤝 Contributing
Feel free to fork this repository and submit pull requests if you want to improve the game logic or add new features!

---
*Created by [robin-demesmaeker06](https://github.com/robin-demesmaeker06)*